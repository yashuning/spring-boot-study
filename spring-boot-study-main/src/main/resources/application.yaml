server:
  port: 8080 # 内嵌的 Tomcat 端口号。默认值为 8080。

spring:
  # elasticsearch
  elasticsearch:
    rest:
      uris: 8.131.89.92:9200
      username: es
      # password: es
      connection-timeout: 10
      read-timeout: 30

  # mysql
  datasource:
    url: jdbc:mysql://8.131.89.92:3306/my_study_db?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: nys123
    password: nys123
    driver-class-name: com.mysql.jdbc.Driver

# mybatis
mybatis:
  # 是否执行MyBatis xml配置文件的状态检查, 只是检查状态,默认false
  # 指定mybatis配置文件路径
  config-location: classpath:config/mybatis-config.xml
  # 指定mybatis生成包
  # 指定 mapper 文件的位置
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.nys.study.spring.springbootstudy.repository.mysql.entity